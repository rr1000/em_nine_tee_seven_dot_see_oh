<section class="blog">
	<% blog.articles[0...10].each do |article| %>
		<%= link_to article.title, article %> <span><%= article.date.strftime('%b %e') %>
		<%= article.summary %>
	<% end %>
</section>